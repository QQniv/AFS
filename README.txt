# Калькулятор водоочистки (GitHub Pages)

Готов к деплою как статический сайт. Работает оффлайн (PWA), можно добавить на экран iPhone.

## Как развернуть на GitHub Pages
1. Создайте новый **публичный** репозиторий, например `water-calc`.
2. Загрузите в корень репозитория **все файлы** из этой папки (`index.html`, `app.js`, `styles.css`, `manifest.json`, `service-worker.js`, папку `icons/`).
3. В GitHub откройте **Settings → Pages**:
   - Source: **Deploy from a branch**
   - Branch: **main** / root (`/`)
4. Через ~30–60 секунд сайт будет доступен по адресу `https://<ваш_юзернейм>.github.io/water-calc/`.
5. Откройте на iPhone в Safari, нажмите **Поделиться → На экран «Домой»**.

## Редактирование цен/параметров
Все сохраняется в localStorage браузера. Сбросить можно через настройки сайта в браузере.

Удачи! 💧
